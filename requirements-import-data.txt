# Requirements for twitter_csv.py data import
# Core dependencies for loading and processing Twitter CSV data
#
# IMPORTANT: Install in this order:
# 1. pip3 install torch==2.0.1
# 2. pip3 install torch_scatter torch_sparse -f https://data.pyg.org/whl/torch-2.0.1+cpu.html
# 3. pip3 install torch-geometric==2.3.1 pandas numpy

torch==2.0.1
pandas
numpy
torch-geometric==2.3.1

# Note: torch_scatter and torch_sparse should be installed separately with:
# pip3 install torch_scatter torch_sparse -f https://data.pyg.org/whl/torch-2.0.1+cpu.html
