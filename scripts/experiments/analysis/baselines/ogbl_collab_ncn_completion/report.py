"""Render the admitted nine-cell result; refuses incomplete audits."""
import argparse,json
from pathlib import Path
import numpy as np
p=argparse.ArgumentParser();p.add_argument('--audit',type=Path,required=True);p.add_argument('--out',type=Path,required=True);a=p.parse_args();r=json.loads(a.audit.read_text());assert r['complete'] and len(r['rows'])==9
arms=['control','observed','completion'];names={'control':'Joint control','observed':'Observed neighbors','completion':'With completion'}
means={arm:np.mean([x['hits'] for x in r['rows'] if x['arm']==arm]) for arm in arms}
lines=['# Compact NCN and one-step completion: completed comparison','', 'All nine predeclared cells completed. '+('No new arm passed the advancement gate; stop without expansion, refit or test scoring.' if r['selected_arm_for_replication'] is None else 'A new arm passed the validation gate; historical replication remains required before any further claim.'),'', '| Model | Mean selected 2018 Hits@50 | Difference from control |','|---|---:|---:|']
for arm in arms:lines.append(f'| {names[arm]} | {100*means[arm]:.4f}% | {100*(means[arm]-means["control"]):+.4f} pp |')
lines+=['','| Seed | Control | Observed | Completion |','|---|---:|---:|---:|']
for seed in range(3):
    values=[next(x for x in r['rows'] if x['arm']==arm and x['seed']==seed) for arm in arms]
    lines.append('| '+str(seed)+' | '+' | '.join(f'{100*x["hits"]:.4f}% at update {x["step"]}' for x in values)+' |')
lines+=['','## Where the predictions change','','Each stratum below reports its own positive recall at that model’s recomputed full negative cutoff, averaged over all three seeds. Strata overlap and must not be added.','','| Positive stratum | Control | Observed | Completion |','|---|---:|---:|---:|']
for group in ['novel','repeat','zero_aa','nonzero_aa']:
    values=[np.mean([x['strata'][group]['hits'] for x in r['rows'] if x['arm']==arm]) for arm in arms]
    lines.append('| '+group+' | '+' | '.join(f'{100*x:.4f}%' for x in values)+' |')
lines+=['','| Comparison | Seed | Recovered positives | Lost positives | Net |','|---|---:|---:|---:|---:|']
for x in r['paired']:
    s=x['strata']['all'];lines.append(f'| {x["reference"]} → {x["treatment"]} | {x["seed"]} | {s["recovered"]} | {s["lost"]} | {s["recovered"]-s["lost"]:+d} |')
lines+=['','## Interpretation and important limitations','','Across seeds, observed pooling raises nonzero-AA recall from 95.5784% to 98.1719%, but zero-AA recall falls from 19.5867% to 0.6003%. Completion recovers some of the latter (11.5508%) while lowering nonzero-AA recall to 88.8832%. Completion therefore loses overall even against observed pooling, and neither new arm improves novel-collaboration recall over control. These are net ranking outcomes, not causal attribution to individual features.','','The observed-neighbor decoder and completion decoder have the same neural architecture and initialization; their difference tests the declared completion computation. The comparison against the existing joint control is broader: the new decoders replace its 14 handcrafted inputs with learned neighbor pooling. They do not simply add information to an otherwise identical predictor. The results cannot isolate the effect of removing those features from the effect of adding the neighborhood representation.','','This is an NCNC-inspired compact adaptation, not a reproduction of the published NCNC model. The encoder is retained SAGE, the head is a compact MLP, common neighbors are capped at 32 and one-sided candidates at 8 per endpoint with label-independent sampling and population-mass expansion. Completion uses the shared observed decoder at one recursion level with detached sigmoid(logit+log(.1)) weights. Training weights refresh every 10 updates; validation weights are fresh. The fixed prior, sampling approximation, architecture and optimization may limit performance. Do not generalize a negative result here to every NCNC architecture or input combination.','','The 40 inspected misses were not a fitted training subset. All 119,622 historical positives and the same fresh 100,000-negative panel were used. No warm-only filtering, renewed negatives or new tail objective was introduced. Supplied static author features are not historical snapshots.','','## Protocol and evidence integrity','','All nine cells use 2000 updates and validation every 50, earliest standalone maximum per cell, balanced BCE, Adam .001, weight decay .0001, gradient clip 5, 2048 positives/1024 uniform negatives/1024 mined negatives and top 2048 mining refreshed every 10. The advancement rule requires every seed to improve over control and mean gain>=.005. Each arm is judged against control; if both pass, higher mean wins, observed breaks exact ties.','','All 120 control validation measurements exactly reproduce the archived control. All nine selected checkpoint replays are exact and agree with the official OGB evaluator. The independent audit checks artifact hashes, source/cache manifest identity, earliest selections, full-panel sorted-cutoff metrics, all error counts, equal sampling-slot streams and paired encoder initialization. Actual mined-negative identities may differ with model scores.','','Four synthetic contract tests passed: graph/common/candidate membership and exclusions; symmetry, padding, zero-completion equality and finite gradients; sampling caps with mass expansion; exact encoder initialization and model count. Separate cache audits checked 138 training and 139 validation target rows, including capped cases, against graph neighborhoods and checked their represented child links. Full input pair arrays match the admitted panels. Three ten-update training-only smoke cells and a separate concurrent profile passed before production. No validation score was used to adjust the model or schedule after launch.','','The control has 496,385 neural parameters (496,448 conservative inference scalars); both new models have 525,633 neural parameters (525,697 conservative scalars, including a deliberately conservative allowance for old auxiliary state and the fixed prior). Only one selected model is used for inference. No learned node-ID table or external pretrained model is hidden in graph caches. The parameter limit is not a runtime limit: completion additionally scores 1,517,908 cached child rows (including one padding self-pair) for the 160,084-pair validation panel; observed pooling does not.','','| Arm | Mean measured training/evaluation seconds per seed | Peak allocated GPU bytes |','|---|---:|---:|']
for arm in arms:
    cells=[x for x in r['rows'] if x['arm']==arm];lines.append(f'| {names[arm]} | {np.mean([x["seconds"] for x in cells]):.2f} | {max(x["max_gpu_bytes"] for x in cells)} |')
lines+=['',f'Producing revision: {r["revision"]}. Dedicated Tucker branch codex/collab-ncn-completion, worktree /dataMeR1/phil/gfm/prodigy-collab-ncn-completion. Runtime and full checkpoints: /dataMeR1/phil/gfm/ogbl_collab_compact_joint/ncn_completion_v1. Production ran sequentially on GPU 1; other owned GPUs had active jobs. Offline W&B paths and exact source hashes are retained in each result/config. Local integrated source and evidence are on codex/collab-fresh-ensemble in work/ensemble-repo.','','An initial control-archive transfer placed the tar exclusion option after positional arguments. Tar reported the error and included extra W&B files; the expected control artifacts were still present and verified. Subsequent transfers corrected the option order. No training or evidence values changed. A read-only progress command also encountered unavailable rg on Tucker and used grep subsequently.','','## Test and benchmark status','','No 2019 data were opened or scored in this campaign. The prior fresh-negative standalone best test seed remains 70.1569%; the 71.29% objective remains unmet. Previous extensive test exploration, near-complete negative leakage and test-supervised campaigns remain disclosed. 2018 is repeatedly inspected adaptive development data. No leaderboard acceptance claim or submission follows from this experiment. Seed dispersion reflects optimization on shared data, not independent-data confidence.','','Method reference: [NCNC paper](https://proceedings.iclr.cc/paper_files/paper/2024/hash/3efb4bdc6bfe13e1ff95b4407c37961d-Abstract-Conference.html).','']
a.out.write_text('\n'.join(lines))
