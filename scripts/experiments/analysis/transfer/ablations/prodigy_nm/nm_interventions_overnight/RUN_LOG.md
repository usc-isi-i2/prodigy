# Campaign execution log

- 2026-09-04 09:19 UTC: goal activated; deadline 17:19 UTC. Dedicated local and Tucker worktrees created on `codex/nm-interventions-overnight` from fast-launcher revision `9ea87fe`.
- 09:25 UTC: source-restriction tests pass; initial campaign implementation committed and pushed through git.
- 09:27 UTC: first full-graph smoke stopped at CUDA preflight before data load. GPU 0 reports idle/default mode but rejects standalone allocation. GPUs 1–3 allocate successfully. No production training occurred.
- 09:29 UTC: second smoke launched on GPUs 1–3 in `nmi_smoke`; all 17 methods included. No test-source feedback enters stopping or selection.
- 09:33 UTC: second smoke completed all 17 configurations on GPUs 1–3. Every arm trained 20 steps, validated on eight included sources, and wrote a selected checkpoint. Strict reload/fingerprint gate follows before production.
- 09:34 UTC: strict checkpoint reload / finite-weight / eight-source fingerprint gate passed all 17 smoke arms. Production launched in `nmi_train` at revision `6260524e`, GPUs 1–3, four concurrent models/GPU, 48 loader workers. 136 configs queued with rung eight first. GPU 0 remains unavailable; no GPU reset attempted.
- 09:41 UTC: manually rendered and inspected all twelve live eight-source curves (`data/progress_0941.json`, `figures/progress_0941.png`). Losses show rapid initial decline and flattening, with no divergence/non-finite values. Blocked scheduling has recurring loss oscillations consistent with source blocks; these are not grounds to abort. Cross-graph loss is much lower but validation remains comparable, so loss is not used to rank interventions. Existing 2k-check interval and 10k safety cap retained; actual stopping follows source-validation plateau.
- 09:41 UTC: compatibility rules hardened before combination selection: blocked/proportional and cross-graph/degree-matched negatives cannot coexist without overriding the intended individual intervention. Three focused selection tests pass. Idle evaluation worktree advanced to `54f20fdd`; live training remains pinned to `6260524e`. Reports now separate eight-source included and unseen endpoint verdicts, expose missing arms as incomplete, and export not-yet-included source summaries.
- 09:46 UTC: audited every row of the first curve snapshot: TwiBot-20 exposure is zero and source exposure fractions sum to the recorded episode count. Validation inference accounts for approximately 2–5% of elapsed training time in that snapshot (excluding initial cache construction/checkpoint I/O), supporting the 2k validation interval. Observed per-model rate is approximately 14–21 episodes/s across twelve concurrent models; this is a measured snapshot, not an end-to-end completion guarantee.
- 09:44 UTC: launched `nmi_replay` from the separate evaluation checkout at `225e9bfa`. It replays only training-source validation for the first completed production checkpoint, comparing fingerprints and ROC-AUC/accuracy/loss to the original trainer. No test metrics are requested by this gate.
- 09:48 UTC: exported and audited the first nine completed training models, all at rung eight. Selected checkpoints, effective configs, validation histories, stop reasons and training revision `6260524e` are retained in `data/model_records.json`. Every final exposure record excludes TwiBot-20. These are completed training models, not completed NM results: all arm verdicts remain incomplete pending downstream evaluation.
- 09:48 UTC: the first checkpoint replay passed the first six training sources, then failed its original 1e-6 AUC tolerance on hongkong (`cp_hk`): delta 1.1224856e-6, exactly matching accuracy and loss delta 2.98e-8; episode fingerprint matched. This AUC difference corresponds to four pair-order changes in the macro AUC panel. The first failure remains preserved under `log/replay_gate_v1`. No downstream test run was launched.
- 09:50 UTC: launched `nmi_replay_v2` at evaluation revision `b7f51792`, using three repeat forwards on each original validation panel for the same eligibility checkpoint. AUC tolerance is explicitly 1e-5 (100 times below the intervention threshold); fingerprint identity and 1e-6 accuracy/loss tolerances remain. Numerical-variation diagnosis is pending this replay. Production training remains live and unchanged at `6260524e`.
- 09:52 UTC: manually inspected the second curve grid (`progress_0952`), now covering all 17 eight-source arms. Twelve endpoint models have finished. Losses remain finite; coverage-cycle loss starts rising after its initial decline, consistent with the documented ordered-center traversal caveat, while validation has levelled off. Baseline/composition selected step 8k; source-affine and low-degree selected 2k. No stopping rule changed. Models reaching the cap are recorded as capped, not asserted asymptotically converged. All captured exposure rows still exclude TwiBot-20.
- 09:56 UTC: replay v2 passed all eight training sources with three forwards each. Maximum within-replay ROC-AUC range was 1.4031e-6; maximum discrepancy from the original trainer was 1.9644e-6. Accuracy matched exactly on every source; maximum loss discrepancy was 1.1921e-7. This directly confirms numerical repeat variation at the scale of the original failed 1e-6 AUC check. Detailed records are committed under `data/replay_gate_v2`. Downstream evaluation retains the replay gate for every model/source.
- 09:56 UTC: all 17 eight-source models passed the completed-checkpoint discovery gate. Launched `nmi_nm_r8` from evaluation revision `b7f51792`: 17 models × 8 training-source test targets × 512 fixed episodes, three evaluators (one per GPU 1–3), with per-model/source validation replay before test inference. Output: `/dataMeR1/phil/gfm/prodigy-nmi-eval/log/nm_individual_sources_r8`. Remaining ladder training continues in `nmi_train`. TwiBot-20 test evaluation stays deferred until the training-validation-only combined recipe is frozen.
- 10:02 UTC: first downstream NM cells arrived for ukraine, with observed 512-episode forward time about 22–34 seconds per model. Three evaluator workers are CPU-active and memory remains available; no evaluator or training failure is present. Per-source results are provisional until their full comparison panel exists. Plotting now suppresses incomplete graph-panel means rather than plotting an available-target mean under a complete-panel label.
- 10:05 UTC: exported 30 completed training models and the first 16 downstream NM cells (ukraine only). All 16 cells share the same test-episode fingerprint, training revision `6260524e`, and evaluation revision `b7f51792`. Verified that every arm still reports incomplete and no partial included-source mean is plotted or exported as a complete-panel score.
- 10:13 UTC: created a separate read/analysis worktree at `/dataMeR1/phil/gfm/prodigy-nmi-analysis` from `3c85c8b2`; production and evaluator revisions stay pinned. Export now includes parameter counts, memory, runtime, and stopping metadata. Wider model: 4,860,034 registered parameters versus 1,640,514 baseline (2.96x). Of the first 37 completed models, 29 reached the cap and 20 still gained >0.001 at the last validation check; report these as budget-bounded comparisons, not universal convergence claims. The common predeclared cap remains unchanged across arms.
- 10:16 UTC: manually reviewed all 17 completed rung-one curves in `figures/progress_r1_review.png`. Losses show smooth initial decline and subsequent flattening, with no divergence. The budget diagnostic ends at its planned 1,250-episode cap. Every captured exposure row is entirely ukraine, with zero TwiBot-20 exposure.
- 10:27 UTC: the initial NM sweep completed all 136 cells (17 methods × 8 included targets). Verified the exact expected grid, 512 episodes per cell, common per-target fingerprints, and one checkpoint hash/step per model. No TwiBot-20 test cells exist. Full-ladder arm statuses remain incomplete.
- 10:29 UTC: launched `nmi_nm_ladder` at evaluator revision `4e36a123`, two workers per GPU 1–3. The completed endpoint artifacts were copied into the accumulating `/dataMeR1/phil/gfm/prodigy-nmi-eval/log/nm_individual` result directory; the original endpoint directory remains intact. Subsequent collection must use the accumulation directory alone to avoid duplicate cells. New invocation metadata archives the old plan/status and marks the new batch running; lifecycle failure/zero-worker tests passed.
- 10:33 UTC: rendered and manually inspected the endpoint graph-by-graph effect matrix. Included-source mean gains are objective +0.002528, alignment +0.002235, composition +0.001848. These are seed-zero endpoint observations, not combination choices or full-ladder/unseen verdicts. Per-graph tradeoffs are explicit in `data/endpoint_by_graph.csv` and its figure.

- 10:38 UTC: manually inspected all 17 completed rung-two curves (`data/progress_r2_review.json`, `figures/progress_r2_review.png`). Losses are finite and decline/flatten; blocked scheduling retains expected oscillations. Source-affine validation falls after 4k; the selected checkpoint is 4k and plateau stopping occurs at 8k. Every captured exposure row is restricted to ukraine/covid and sums to its episode count. Exported 73 completed training models and 167 NM cells from the accumulation directory; both production and ladder evaluation remain live. Capped runs remain labelled bounded comparisons.
