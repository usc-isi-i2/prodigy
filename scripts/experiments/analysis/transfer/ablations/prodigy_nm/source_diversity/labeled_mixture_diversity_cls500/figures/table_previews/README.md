# Mixture-diversity table previews

These PNGs are visual previews of the locally generated
`mixture_diversity_results.xlsx` workbook. The numerical evidence is canonical
in the tracked CSV and JSONL files under `../../data/`; the editable workbook
remains a local presentation export because spreadsheet files are not part of
the repository's tracked analysis-artifact policy.
