# One independently nominated natural-pair K/V prediction

7 September2026. Frozen before new K/V outcomes. Private decision; not a
manuscript expansion. Source **Ukraine**, target **TwiBot20**; historical seed0
step2500 checkpoint from the existing message-scale inventory, both original
and fresh128-episode streams. Preserve the recorded architecture, inputs and
inference protocol. No new training, episode sampling or checkpoint choice.

Independent contribution review nominated this pair over a second political
source because it changes both source and task and tests useful context,
not another instance of the known political failure. The existing role map's
roughly−5.4 pooled-AUC-point support-removal effect motivated selection. That
known endpoint is not a prospective result. The unseen outcome is the K/V
pathway ordering in within-episode AUC.

For each stream define delta relative to intact within-episode AUC. Require:

1. values-only delta <0;
2. values-only delta <= keys-only delta −.01.

Both inequalities must hold in EACH stream. One point is a practical separation,
not statistical significance. No pooled-AUC substitution, source search or
threshold adjustment after outcomes. Report accuracy and actual key-attention
TV; an inert key intervention would weaken interpretation even if the gate
passes. Retain intact, removed, keys-only, values-only and joint K/V outcomes.

Reuse the audited support-local donor helper. Values-only must preserve native
label attention exactly. Final query vectors must remain exact in every
condition. Joint K/V must reproduce removed-label vectors and logits exactly.
Native intact/removed logits must reproduce the earlier saved scale endpoints.
Input, donor and weight/buffer identities must remain unchanged. Write the
prediction into the remote run protocol before any forward. Artifacts private.

Passing would extend the finding that transported support values, rather than
attention changes alone, carry both useful and harmful context across these
two pairings. It would not predict utility signs, explain natural bot training
decline, establish an independent-architecture mechanism, or demonstrate an
adaptation method. Failure retains a political-specific value-path localization
and stops the proposed shared-pathway generalization; no replacement pair.

## Completed result and advisor decision

7 September 2026, code 65946f82. The frozen prediction passes both streams.
Aggregate evidence is in `data/kv_generality_20260907/`: metrics, geometry,
receipts, protocol, prediction_assessment and DONE JSON. No new training.

| Stream | Keys-only AUC delta | Values-only AUC delta | Joint AUC delta | Key attention TV |
| --- | ---: | ---: | ---: | ---: |
| Original | +1.1882 | -6.4182 | -6.1957 | .071305 |
| Fresh | +.9277 | -4.9750 | -5.3277 | .072133 |

Deltas are within-episode AUC percentage points, relative to intact; not pooled
AUC, confidence intervals, or training-seed replication. There are 128 episodes
per condition per stream. Values-only accuracy drops 4.7201/3.1901 points;
keys-only accuracy rises .9115/.3906 points. The practical key-value separation
is 7.6063/5.9028 AUC points, exceeding the frozen one-point condition.

All 10 cells are complete. The two native endpoints and joint-removal endpoints
are bit-exact; 320 fixed-query checks pass. Values-only attention TV is exactly
zero in every episode, while the key intervention is engaged. Local aggregate
verification checks complete condition grids, receipt invariants and recomputes
deltas directly from metrics rather than trusting the assessment boolean.

Independent review and advisor decision: stop this experimental block and
pursue the scoped mechanism paper. The new observation is the opposite-sign
routing/content consequence on bots, alongside beneficial value replacement on
political classification. It is not simply the architectural fact that values
matter. Transported support content can carry both benefits and failures with
attention and queries fixed. The nine-source map supplies role-outcome breadth,
the two pairings supply pathway localization, and 50k supplies configuration
persistence; these are distinct evidentiary levels.

Do not claim this resolves norm versus direction versus operating range, predicts
context utility, explains the natural bot training decline, or makes suppression
a useful method. Public role coupling and the 50k ranking/accuracy disagreement
remain substantive boundaries. The intended high-contribution goal stays open;
passing this test alone is not a publication-level novelty certification.

## Paired episode and query accounting from saved outputs

Read-only Tucker analysis, no new forwards. `analyze_kv_examples.py --root
/dataMeR1/phil/gfm/prodigy-kv-generality/log/ukraine_bot_kv_20260907` reproduces
`data/kv_generality_20260907/example_accounting.json`. The private prediction
tensors stay on Tucker. This is descriptive accounting conditional on the
fixed checkpoint and cached episodes, not independent-account inference.

Values-only harms within-episode AUC in106/128 original episodes and95/128
fresh episodes; helps16/25 and ties6/8. Median deltas are -4.861/-4.167 points.
Excluding the worst episode per stream leaves mean effects -6.157/-4.817,
versus full means -6.418/-4.975. The aggregate loss is not driven by one
extreme episode. Keys help74/73 episodes and harm33/36 (rest ties).

At the query-decision level, values lose259/222 initially correct predictions
and gain114/124, yielding net -145/-98 out of3072 per stream. Keys lose47/64
and gain75/76, net +28/+12. Values change373/346 decisions; keys122/140;
27/28 decisions change under both. This is a count of repeated query occurrences,
not unique accounts, and no test of independent effect incidence is claimed.

For transparent example selection, take the median-AUC-loss episode among
episodes harmed by values, then its first three intact-correct/value-wrong
query offsets. Original selects episode111; fresh selects47. These are
illustrations conditional on harm, not randomly sampled successes or semantic
biography inspection. Fresh query offset1147 has local class1 and logit1-logit0
margin +.497 intact, +.860 keys-only, -.362 values-only, -.265 joint. Fresh
offset1131 has local class0: -.185 intact, -.285 keys-only, +.133 values-only.
Original offset2664 (class0): -.908 intact, -.880 keys-only, +.219 values-only.
Thus the same unchanged query can retain a correct decision under altered
routing but cross the decision boundary under altered support content.

Important accounting limit: the zero count of binary ranking pairs simultaneously
hurt by values and helped by keys is not a discovery. Relative to one untied
binary baseline, a pair cannot both lose and gain ranking credit. Do not use
that field to argue pathway independence. Prediction-change overlap likewise
does not establish mechanistic independence. Retain the full-factorial
interaction qualifications in FINDINGS_KV_CLAIM_AUDIT.md.
