# Non-result Tucker worktree dispositions, 2026-09-12

These worktrees produced durable setup code or preflight checks but no admissible
scientific comparison. Recording that negative disposition prevents their runtime
directories from being mistaken for unpublished evidence.

- `prodigy-msize`: `nm_model_size_eval` produced setup/protocol scaffolding only and
  no admissible completed comparison. Its tiny runtime artifacts are intentionally
  discarded without a scientific claim.
- `prodigy-nm2sched`: the two-source schedule pilot has no validated comparison
  table. It is exploratory/incomplete and its runtime is intentionally discarded
  rather than cited.
- `prodigy-publickg-assets`: this worktree staged and verified official public-KG
  assets. The durable graph registration and staging code are already committed;
  its console log is non-scientific runtime.
- `prodigy-publickg-evalprep`: this worktree performed layout preflight without
  model predictions. The committed preflight code is the durable output; its
  runtime supports no performance claim.
