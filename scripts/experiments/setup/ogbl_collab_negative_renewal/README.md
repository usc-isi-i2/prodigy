# Negative renewal, frozen v1

Run `bash scripts/experiments/setup/ogbl_collab_negative_renewal/launch.sh` in its dedicated Tucker worktree and tmux session. GPU0 only; prodigy; offline W&B. `run.py dry-run` prints the frozen contract. Six 2000-update cells: fixed and renewed, seeds0/1/2. All original architecture, optimizer, positive/index RNG, standardization, and historical graph invariants retained. Independent100k panels renew every50 updates, mining2048 every10. Probe pool40 never optimized. Validation selection is standalone earliest maximum for both arms. Main advancement requires every paired gain positive and mean gain >=0.005; no test scoring exists. Prior repeated test exploration remains disclosed and leaderboard acceptance unresolved.

Feature caching copies exact make_year arithmetic, shares historical graph preprocessing, and checks bitwise parity on1000 archived negatives before generating40 independent pools. Raw training graph CSV and train.pt read; no test reader. Same-year positives and validation negatives excluded; no test-dependent filtering. Optimization RNG hash covers positive, uniform, and hard-slot indices; actual hard identities differ. Full histories, best checkpoints, scores, official-evaluator checks, panel hashes, and offline W&B retain evidence.
